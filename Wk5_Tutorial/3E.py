keys = ['Ten', 'Twenty', 'Thirty']
values = [10, 20, 30]

dictionary = dict(zip(keys,values))
print(dictionary)

# Zip Function - Used to gather lists : 'Keys' and 'Values'
# Outside Zip Function the Keyword 'dict'
# Print New dict to Terminal so we can see the Result